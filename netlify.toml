[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Security: Disable secrets scanning for specific paths if needed
# SECRETS_SCAN_OMIT_PATHS = "dist/**"
